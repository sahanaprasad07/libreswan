<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE refentry PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
"http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd"
[<!ENTITY % entities SYSTEM "entities.xml">%entities;]>
<refentry>
  <refmeta>
    <refentrytitle>IPSEC-MLDSASIGKEY</refentrytitle>
    <manvolnum>8</manvolnum>
    <refmiscinfo class='date'>14 Aug 2026</refmiscinfo>
    <refmiscinfo class="source">Libreswan</refmiscinfo>
    <refmiscinfo class="version">@@IPSECVERSION@@</refmiscinfo>
    <refmiscinfo class="manual">Executable programs</refmiscinfo>
  </refmeta>
  <refnamediv id='name'>
    <refname>ipsec-mldsasigkey</refname>
    <refpurpose>generate ML-DSA signature key</refpurpose>
  </refnamediv>
  <!-- body begins here -->
  <refsynopsisdiv id='synopsis'>
    <cmdsynopsis>
      <command>ipsec mldsasigkey</command>
      <arg choice='opt'>--verbose</arg>
      &nss_opts;
      <sbr/>
      <arg choice='opt'>--hostname <replaceable>hostname</replaceable></arg>
      <arg choice='opt'>paramset-name</arg>
    </cmdsynopsis>
  </refsynopsisdiv>


  <refsect1 id='description'>
    <title>DESCRIPTION</title>
    <para>
      <command>mldsasigkey</command>
      generates an ML-DSA public/private key pair, suitable for digital signatures, using the
      parameter set named by <emphasis>paramset-name</emphasis>. Currently it only accepts
      <emphasis>ml-dsa-44</emphasis>, <emphasis>ml-dsa-65</emphasis>, and
      <emphasis>ml-dsa-87</emphasis>; the default is <emphasis>ml-dsa-65</emphasis>.
    </para>

    <para>
      <command>mldsasigkey</command> is only built when &Libreswan; is built with
      <option>USE_MLDSA=true</option>, and requires an &NSS; build with ML-DSA
      sign/verify/key-generation support.
    </para>

    <para>
      The <option>--verbose</option> option makes <emphasis>mldsasigkey</emphasis>
      give a running commentary on standard error. By default, it works in
      silence until it is ready to generate output.
    </para>

    <para>
      The <option>--seeddev</option> option specifies a
      source for random bits used to seed the crypto library's
      RNG. The default is <filename>/dev/random</filename> (see
      &random.4;).
    </para>

    <para>
      The <option>--seedbits</option> option specifies how many seed bits are pulled from
      the random device to seed the NSS PRNG. The default of 480bit comes from FIPS requirements.
      Seed bits are rounded up to a multiple of 8.
    </para>

    <para>
      The use of a different random device or a reduction of seedbits from the default value is
      prevented when the system is running in FIPS mode.
    </para>

    <para>
      The <option>--nssdir</option> option specifies the directory to use for the nss database.
      This is the directory where the NSS certificate, key and security modules databases reside. The
      default value is <filename>@@IPSEC_NSSDIR@@</filename>.
    </para>

    <para>
      The <option>--nsspw</option> option specifies the nss cryptographic module authentication
      password if the NSS module has been configured to require it.  A password is required by hardware
      tokens and also by the internal software token module when configured to run in FIPS mode.
      If the argument is <emphasis>@@IPSEC_CONFDDIR@@</emphasis><filename>/nsspassword</filename>,
      the password comes from that file; otherwise argument is the password.
    </para>
  </refsect1>

  <refsect1 id='examples'><title>EXAMPLES</title>
  <variablelist>
    <varlistentry>
      <term>
	<command>ipsec mldsasigkey --verbose ml-dsa-87</command>
      </term>
      <listitem>
	<para>generates an ml-dsa-87 key and stores this key in the NSS database.
	The public key can then be extracted and edited into the <filename>ipsec.conf</filename> (see
	&ipsec-showhostkey.8;).
	</para>
      </listitem>
    </varlistentry>
  </variablelist>
  </refsect1>

  <refsect1 id='files'>
    <title>FILES</title>
    <para>
      /dev/random, /dev/urandom
    </para>
  </refsect1>

  <refsect1 id='see_also'>
    <title>SEE ALSO</title>
    <para>
      &random.4;,
      &rngd.8;,
      &ipsec-showhostkey.8;,
      <emphasis>FIPS 204, Module-Lattice-Based Digital Signature Standard</emphasis>
    </para>
  </refsect1>

  <refsect1 id='history'>
    <title>HISTORY</title>
    <para>
      Written for the Libreswan Project, following the shape of
      &ipsec-ecdsasigkey.8; and &ipsec-rsasigkey.8;.
    </para>
  </refsect1>

  <refsect1 id='bugs'>
    <title>BUGS</title>
    <para>
      <command>mldsasigkey</command>'s run time is difficult
      to predict, since <filename>/dev/random</filename> output can
      be arbitrarily delayed if the system's entropy pool is low on
      randomness.
    </para>
  </refsect1>

  <refsect1 id='author'>
    <title>AUTHOR</title>
    <simplelist type='inline'>
      <member><author>&SahanaPrasad;</author></member>
    </simplelist>
  </refsect1>
</refentry>
